# Aurora HR – UI Prototype (Vite + React)

## Run locally
```bash
npm install
npm run dev
```

## What's inside
- Clickable navigation for **every** module + section.
- Guided Create sheet.
- Dashboard charts (line, bar, pie) using Recharts.
- Theme picker: Cloud / Midnight / Oasis / Desert / Plum.

> UI prototype only (no backend).
